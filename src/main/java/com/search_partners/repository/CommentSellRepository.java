package com.search_partners.repository;

import com.search_partners.model.CommentSell;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CommentSellRepository extends JpaRepository<CommentSell, Long> {
}
