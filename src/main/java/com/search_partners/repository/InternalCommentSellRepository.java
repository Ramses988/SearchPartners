package com.search_partners.repository;

import com.search_partners.model.InternalCommentSell;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InternalCommentSellRepository extends JpaRepository<InternalCommentSell, Long> {
}
